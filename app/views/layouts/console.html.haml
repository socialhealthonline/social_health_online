!!!
%html{lang: 'en'}
  = render 'head'
  %body{class: "#{controller_name} #{action_name}"}
    = render 'nav_bar'
    .main-container
      %section.space-sm
        .container
          .row
            .col-12.col-md-3.mb-1.order-md-1
              .card.card-sm
                .list-group.list-group-flush
                  = link_to console_members_path, class: 'list-group-item d-flex justify-content-between' do
                    %div
                      %i.fas.fa-users
                      Members
                  = link_to console_affiliates_path, class: 'list-group-item d-flex justify-content-between' do
                    %div
                      %i.far.fa-handshake
                      Affiliates
                  = link_to console_news_index_path, class: 'list-group-item d-flex justify-content-between' do
                    %div
                      %i.far.fa-newspaper
                      News
                  = link_to '#', class: 'list-group-item d-flex justify-content-between' do
                    %div
                      %i.fas.fa-shield-alt
                      Admins
            .col-12.col-md-9.mb-1.order-md-2
              - unless flash.empty?
                = render partial: 'flash', locals: { flash: flash }
              = yield

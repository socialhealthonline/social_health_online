- page_title 'News'

.container
  .row.py-5
    .col-lg
      %h2.text-info News

      - @news.each do |news|
        .mb-3
          %p.h5= news.title
          %p= date_for_news(news.updated_at)
          %p= simple_format(news.body)
      %p= paginate @news

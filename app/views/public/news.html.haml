- page_title 'News'

.container
  .row.py-5
    .col-lg
      %h2.text-info News

      - @news.each do |news|
        .mb-3
          %p.h5= news.title
          %p= news.updated_at.strftime("%d/%m/%Y")
          %p= simple_format(news.body)
      %p= paginate @news

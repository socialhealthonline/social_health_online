You have a new notification

-----------------------------------------------
Subject: <%= @notification.subject.html_safe? ? @notification.subject : strip_tags(@notification.subject) %>
Notification: <%= @notification.body.html_safe? ? @notification.body : strip_tags(@notification.body) %>
-----------------------------------------------

Visit <%= root_url %> to check your messages.

You have a new reply

-----------------------------------------------
Subject: <%= @subject %>
Reply: <%= @message.body.html_safe? ? @message.body : strip_tags(@message.body) %>
-----------------------------------------------

Visit <%= root_url %> to check your messages.

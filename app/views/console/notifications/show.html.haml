%nav(aria-label="breadcrumb")
  %ol.breadcrumb
    %li.breadcrumb-item= link_to 'News', console_notifications_path
    %li.breadcrumb-item.active(aria-current="page")= @notification.title

.d-flex.justify-content-between.flex-wrap.flex-md-nowrap.align-items-top.mb-3.border-bottom
  %h1.h3.brand-primary= @notification.title

%p
  = simple_format(@notification.body)

.mb-3
  = link_to console_notifications_path, class: 'btn btn-outline-primary'do
    %i.fas.fa-arrow-left
    Back
  &nbsp;
  = link_to edit_console_notification_path(@notification), class: 'btn btn-outline-primary' do
    %i.far.fa-edit
    Edit
  &nbsp;
  = link_to console_notification_path(@notification), method: :delete, class: 'btn btn-outline-danger', data: { confirm: 'Please confirm that you wish to delete this notification. Press OK to continue.' } do
    %i.far.fa-trash-alt
    Delete
